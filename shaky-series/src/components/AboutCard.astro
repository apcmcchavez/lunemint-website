<section id="about" class="about-section">
  <article class="about-card">
    <div class="about-left">
      <p class="about-label">/ about me</p>

      <img
        class="artist-pic"
        src="/images/raw_pictures/artist-pic.png"
        alt="Lunemint artist"
        loading="lazy"
        decoding="async"
        width="400"
        height="510"
      />

      <img
        class="barcode"
        src="/images/about/barcode-for-the-about-me-card.svg"
        alt=""
        aria-hidden="true"
        loading="lazy"
        decoding="async"
        width="350"
      />
    </div>

    <div class="about-main">
      <h2>lunemint</h2>
      <p class="credential">artist credentials</p>

      <img
        class="design-line"
        src="/images/about/design-line-in-the-about-me-card.svg"
        alt=""
        aria-hidden="true"
        loading="lazy"
        decoding="async"
      />

      <div class="bio-block">
        <h3>BIO</h3>
        <p class="bio-text">
          A BS Computer Science student with a passion for design and
          creativity. I create visuals that help students, organizations, and
          businesses communicate their ideas effectively. Whether it’s a Canva
          graphic, a polished presentation, a standout resume, or a specific
          design, I put care and intention into every project.
        </p>
      </div>

      <div class="social-row">
        <a href="https://www.instagram.com/madeby.lunemint/" target="_blank" rel="noopener noreferrer" aria-label="Lunemint on Instagram">
          <img
            src="/images/icons/instagram-logo-about-me-card.svg"
            alt="Instagram"
            loading="lazy"
            decoding="async"
            width="96"
            height="96"
          />
        </a>

        <a href="#" aria-label="Lunemint on Gumroad (placeholder)">
          <img
            src="/images/icons/gumroad-logo-about-me-card.svg"
            alt="Gumroad"
            loading="lazy"
            decoding="async"
            width="96"
            height="96"
          />
        </a>

        <a href="#" aria-label="Lunemint on Raket.ph (placeholder)">
          <img
            src="/images/icons/raketph-logo-about-me-card.svg"
            alt="Raket.ph"
            loading="lazy"
            decoding="async"
            width="96"
            height="96"
          />
        </a>

        <a href="#" aria-label="Lunemint on Etsy (hidden)">
          <img
            class="hidden-for-now"
            src="/images/icons/etsy-logo-about-me-card.svg"
            alt="Etsy"
            loading="lazy"
            decoding="async"
            width="96"
            height="96"
          />
        </a>

        <a href="#" aria-label="Lunemint on Printify (hidden)">
          <img
            class="hidden-for-now"
            src="/images/icons/printify-logo-about-me-card.svg"
            alt="Printify"
            loading="lazy"
            decoding="async"
            width="96"
            height="96"
          />
        </a>

        <!-- To re-enable these later: update the href values and/or remove `hidden-for-now` class. -->
      </div>
    </div>

    <img
      class="about-logo"
      src="/images/logos/lunemint-scalloped-logo.svg"
      alt=""
      aria-hidden="true"
      loading="lazy"
      decoding="async"
      width="220"
      height="220"
    />

    <img
      class="sparkles"
      src="/images/about/cluster-star-for-the-about-me-card.png"
      alt=""
      aria-hidden="true"
      loading="lazy"
      decoding="async"
      width="255"
    />
  </article>
</section>

<style>
  .about-section {
    padding: 84px 1rem 96px;
    background-color: #a879bb;
    background-image: url("/images/about/about-me-stars-bg.png");
    background-size: cover;
    background-position: center;
  }

  .about-card {
    width: min(1540px, 94vw);
    min-height: 720px;
    margin: 0 auto;
    position: relative;

    display: grid;
    grid-template-columns: 430px minmax(0, 1fr);
    gap: 16px;

    background: #f7ffea;
    border-radius: 44px;
    padding: 42px 64px 50px 46px;
    box-sizing: border-box;

    box-shadow:
      22px 22px 0 #24142f,
      42px 38px 0 rgba(36, 20, 47, 0.16);
  }

  .about-left {
    position: relative;
    z-index: 2;
  }

  .about-label {
    margin: 0 0 18px;
    font-family: var(--font-gladiola);
    font-size: clamp(2rem, 2.4vw, 2.65rem);
    line-height: 0.95;
    font-style: italic;
    font-weight: 800;
    color: #a230a4;
  }

  .artist-pic {
    display: block;
    width: 400px;
    height: 510px;
    object-fit: cover;
    border: 28px solid #f4ecc4;
    box-sizing: border-box;
  }

  .barcode {
    display: block;
    width: 350px;
    height: auto;
    object-fit: contain;
    margin: 18px 0 0;
  }

  .about-main {
    position: relative;
    z-index: 2;
    padding-top: 78px;
    padding-right: 250px;
  }

  .about-main h2 {
    margin: 0;
    font-family: var(--font-sergio);
    font-size: clamp(5.8rem, 7.4vw, 7.35rem);
    font-weight: 900;
    line-height: 0.78;
    color: #32148a;
  }

  .credential {
    margin: 10px 0 18px;
    font-family: var(--font-gladiola);
    font-size: clamp(3rem, 4.8vw, 4.2rem);
    font-style: italic;
    font-weight: 800;
    line-height: 0.82;
    color: #cbc5e5;
  }

  .design-line {
    display: block;
    width: min(650px, 100%);
    height: auto;
    object-fit: contain;
    object-position: left;
    margin: 0 0 30px;
  }

  .bio-block {
    position: relative;
    padding-left: 34px;
  }

  .bio-block::before {
    content: "";
    position: absolute;
    left: 0;
    top: 8px;
    bottom: 0;
    width: 6px;
    background-image: repeating-linear-gradient(
      to bottom,
      #32148a 0 8px,
      transparent 8px 14px
    );
  }

  .about-main h3 {
    margin: 0 0 16px;
    font-family: var(--font-sergio);
    font-size: clamp(3rem, 3.8vw, 3.75rem);
    font-weight: 900;
    line-height: 0.8;
    color: #a173af;
  }

  .bio-text {
    max-width: 820px;
    margin: 0;
    font-family: var(--font-body);
    font-size: clamp(1.45rem, 1.72vw, 1.85rem);
    line-height: 1.34;
    color: #290187;
  }

  .social-row {
    display: flex;
    gap: 28px;
    margin-top: 30px;
    flex-wrap: wrap;
  }

  .social-row img {
    width: 96px;
    height: 96px;
    object-fit: contain;
    transition:
      transform 220ms ease,
      filter 220ms ease;
  }

  .social-row a {
    display: inline-block;
  }

  .hidden-for-now {
    display: none !important;
  }

  .social-row img:hover {
    transform: translateY(-8px) rotate(-4deg) scale(1.06);
    filter: drop-shadow(0 10px 8px rgba(41, 0, 135, 0.28));
  }

  .about-logo {
    position: absolute;
    top: 30px;
    right: 48px;
    width: 220px;
    height: 220px;
    z-index: 3;
  }

  .sparkles {
    position: absolute;
    right: 22px;
    bottom: 18px;
    width: 255px;
    height: auto;
    object-fit: contain;
    z-index: 1;
  }

  @media (max-width: 1250px) {
    .about-card {
      width: min(1120px, 92vw);
      min-height: 650px;
      grid-template-columns: 330px minmax(0, 1fr);
      gap: 18px;
      padding: 36px 48px 46px 36px;
    }

    .about-label {
      font-size: clamp(1.55rem, 2vw, 2rem);
    }

    .artist-pic {
      width: 310px;
      height: 430px;
      border-width: 20px;
    }

    .barcode {
      width: 290px;
      margin-top: 18px;
    }

    .about-main {
      padding-top: 56px;
      padding-right: 150px;
    }

    .about-main h2 {
      font-size: clamp(4.2rem, 6vw, 5.5rem);
    }

    .credential {
      font-size: clamp(2.4rem, 4vw, 3.2rem);
    }

    .bio-text {
      font-size: clamp(1.1rem, 1.45vw, 1.35rem);
    }

    .about-logo {
      width: 155px;
      height: 155px;
      right: 38px;
      top: 34px;
    }

    .sparkles {
      width: 190px;
      right: 18px;
      bottom: 18px;
    }

    .social-row {
      gap: 18px;
    }

    .social-row img {
      width: 74px;
      height: 74px;
    }
  }

  @media (max-width: 900px) {
    .about-section {
      padding: 64px 1rem 80px;
    }

    .about-card {
      grid-template-columns: 1fr;
      width: min(640px, 92vw);
      min-height: auto;
      padding: 36px;
      gap: 30px;
      border-radius: 32px;
      box-shadow:
        14px 14px 0 #24142f,
        28px 26px 0 rgba(36, 20, 47, 0.16);
    }

    .about-left {
      display: grid;
      justify-items: center;
    }

    .about-label {
      justify-self: start;
    }

    .artist-pic {
      width: min(410px, 100%);
      height: auto;
      aspect-ratio: 0.78 / 1;
    }

    .barcode {
      width: min(360px, 85%);
    }

    .about-main {
      padding: 0;
      text-align: center;
    }

    .design-line {
      margin-left: auto;
      margin-right: auto;
    }

    .bio-block {
      padding-left: 0;
    }

    .bio-block::before {
      display: none;
    }

    .bio-text {
      margin: 0 auto;
      font-size: clamp(1.05rem, 4vw, 1.25rem);
    }

    .social-row {
      justify-content: center;
    }

    .social-row img {
      width: 76px;
      height: 76px;
    }

    .about-logo {
      position: static;
      width: 140px;
      height: 140px;
      justify-self: center;
      order: -1;
    }

    .sparkles {
      position: absolute;
      right: 0;
      bottom: 0;
      width: 160px;
      height: auto;
      object-fit: contain;
      z-index: 1;
    }
  }

  @media (max-width: 480px) {
    .about-card {
      padding: 24px 16px 36px;
      border-radius: 24px;
      box-shadow:
        8px 8px 0 #24142f,
        16px 14px 0 rgba(36, 20, 47, 0.16);
      gap: 20px;
    }

    .artist-pic {
      border-width: 14px;
    }

    .barcode {
      width: 180px;
    }

    .about-logo {
      width: 100px;
      height: 100px;
    }

    .sparkles {
      width: 100px;
    }

    .social-row img {
      width: 64px;
      height: 64px;
    }
  }
</style>
