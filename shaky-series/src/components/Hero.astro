<section class="hero">
  <div class="hero-content">
    <div class="hero-copy">
      <p class="hero-kicker">Creative Design Desk</p>

      <h1>
        <span class="white-line">Your idea called.</span>
        <span class="lavender-line">It wants a</span>
        <span class="glow-word">
          glow-up
          <img
            src="/images/hero/glow-up-underline.svg"
            alt=""
            aria-hidden="true"
          />
        </span>
      </h1>

      <p class="hero-description">
        A tiny celestial design desk for Canva graphics, presentations, and
        creative digital pieces.
      </p>
    </div>

    <div class="hero-logo-wrap">
      <img
        src="/images/logos/lunemint-scalloped-logo.svg"
        alt="Lunemint logo"
      />
    </div>
  </div>

  <img
    class="mountains"
    src="/images/hero/watercolor-mountains-hero-bg.webp"
    alt=""
    aria-hidden="true"
  />
</section>

<style>
  .hero {
    min-height: 620px;
    position: relative;
    overflow: hidden;
    background-color: #25004f;
    background-image: url("/images/hero/stars-hero-bg.webp");
    background-size: cover;
    background-position: center top;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr 0.82fr;
    align-items: center;
    gap: 2rem;
    padding: 115px 7.5% 165px;
  }

  .hero-kicker {
    margin: 0 0 0.6rem;
    font-family: var(--font-sergio);
    font-size: clamp(1.45rem, 2.3vw, 30.5px);
    font-weight: 900;
    color: #a330a5;
    text-shadow: 2px 2px 0 rgba(41, 0, 135, 0.45);
  }

  .hero h1 {
    margin: 0;
    line-height: 0.86;
    letter-spacing: -0.055em;
  }

  .hero h1 span {
    display: block;
  }

  .white-line,
  .lavender-line {
    font-family: var(--font-serif);
    font-size: clamp(3.2rem, 6.2vw, 71.1px);
    font-weight: 600;
  }

  .white-line {
    color: #f8faf0;
  }

  .lavender-line {
    color: #8e7cca;
  }

  .glow-word {
    width: fit-content;
    position: relative;
    margin-left: 38%;
    margin-top: -0.98em;
    font-family: var(--font-gladiola);
    font-size: clamp(3rem, 5.8vw, 66.4px);
    font-style: italic;
    font-weight: 700;
    color: #ffdc7e;
    text-shadow:
      0 0 4px rgba(255, 220, 126, 0.9),
      0 0 10px rgba(255, 220, 126, 0.45),
      0 0 18px rgba(255, 220, 126, 0.25),
      2px 2px 0 #6e2c92;
  }

  .glow-word img {
    position: absolute;
    left: 6%;
    bottom: -0.18em;
    width: 92%;
    height: auto;
  }

  .hero-description {
    max-width: 520px;
    margin: 1.1rem 0 0;
    font-family: var(--font-body);
    font-size: clamp(0.8rem, 1.2vw, 15.4px);
    line-height: 1.45;
    color: #c8c6e6;
  }

  .hero-logo-wrap {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-logo-wrap img {
    width: clamp(230px, 28vw, 390px);
    filter: drop-shadow(0 14px 12px rgba(0, 0, 0, 0.3));
  }

  .mountains {
    position: absolute;
    left: 0;
    right: 0;
    bottom: -1px;
    z-index: 3;
    width: 100%;
    pointer-events: none;
  }

  @media (max-width: 760px) {
    .hero {
      min-height: 720px;
    }

    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 70px 1.4rem 170px;
    }

    .glow-word {
      margin-left: auto;
      margin-right: auto;
      margin-top: 0.05rem;
    }

    .hero-description {
      margin-left: auto;
      margin-right: auto;
    }
  }
</style>
