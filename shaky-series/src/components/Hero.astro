<section class="hero">
  <div class="hero-content">
    <div class="hero-copy">
      <p class="hero-kicker">Creative Design Desk</p>

      <h1>
        <span class="white-line">Your idea called.</span>

        <span class="lavender-line">
          <span class="wants-text">It wants a </span>
          <span class="glow-word">
            glow-up.
            <img
              src="/images/hero/glow-up-underline.svg"
              alt=""
              aria-hidden="true"
            />
          </span>
        </span>
      </h1>

      <p class="hero-description">
        A tiny celestial design desk for Canva graphics, presentations, and
        creative digital pieces.
      </p>
    </div>

    <div class="hero-logo-wrap">
      <img
        src="/images/logos/lunemint-scalloped-logo.svg"
        alt="Lunemint logo"
      />
    </div>
  </div>

  <img
    class="mountains"
    src="/images/hero/watercolor-mountains-hero-bg.webp"
    alt=""
    aria-hidden="true"
  />
</section>

<style>
  .hero {
    min-height: 940px;
    position: relative;
    overflow: hidden;

    background-color: #25004f;
    background-image: url("/images/hero/stars-hero-bg.webp");
    background-size: cover;
    background-position: center top;
  }

  .hero-content {
    position: relative;
    z-index: 4;

    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(330px, 0.78fr);
    align-items: center;
    gap: 2.2rem;

    min-height: 940px;
    padding: 130px 7.5% 410px;
    box-sizing: border-box;
  }

  .hero-copy {
    width: 100%;
    max-width: 900px;
    text-align: center;
    transform: translateY(18px);
  }

  .hero-kicker {
    margin: 0 0 0.8rem;
    font-family: var(--font-sergio);
    font-size: clamp(1.95rem, 2.8vw, 3rem);
    font-weight: 900;
    color: #a330a5;
    text-shadow: 2px 2px 0 rgba(41, 0, 135, 0.45);
  }

  .hero h1 {
    margin: 0;
    line-height: 0.88;
    letter-spacing: -0.055em;
  }

  .hero h1 > span {
    display: block;
  }

  .white-line,
  .wants-text {
    font-family: var(--font-serif);
    font-size: clamp(4.2rem, 6.9vw, 7.4rem);
    font-weight: 600;
  }

  .white-line {
    color: #f8faf0;
  }

  .lavender-line {
    display: flex;
    align-items: flex-end;
    justify-content: center;
    flex-wrap: nowrap;
    gap: clamp(0.9rem, 1.5vw, 1.7rem);
    color: #8e7cca;
    margin-top: 0.2rem;
  }

  .wants-text {
    color: #8e7cca;
    white-space: nowrap;
  }

  .glow-word {
    display: inline-flex;
    position: relative;
    flex-shrink: 0;

    font-family: var(--font-gladiola);
    font-size: clamp(3.2rem, 5.2vw, 5.9rem);
    line-height: 0.92;
    font-style: italic;
    font-weight: 700;

    color: #ffdc7e;
    white-space: nowrap;

    transform: translateY(0.02em);

    text-shadow:
      0 0 4px rgba(255, 220, 126, 0.9),
      0 0 10px rgba(255, 220, 126, 0.45),
      0 0 18px rgba(255, 220, 126, 0.25),
      2px 2px 0 #6e2c92;
  }

  .glow-word img {
    position: absolute;
    left: -5%;
    bottom: -0.19em;
    width: 110%;
    height: auto;
    pointer-events: none;
  }

  .hero-description {
    max-width: 760px;
    margin: 1.55rem auto 0;
    font-family: var(--font-body);
    font-size: clamp(1rem, 1.3vw, 1.45rem);
    line-height: 1.45;
    color: #c8c6e6;
  }

  .hero-logo-wrap {
    display: flex;
    justify-content: center;
    align-items: center;
    transform: translateY(26px);
  }

  .hero-logo-wrap img {
    width: clamp(340px, 31vw, 520px);
    height: auto;
    filter: drop-shadow(0 16px 14px rgba(0, 0, 0, 0.36));
  }

  .mountains {
    position: absolute;
    left: 0;
    right: 0;
    bottom: -2px;
    z-index: 2;

    width: 100%;
    height: auto;

    display: block;
    pointer-events: none;
  }

  @media (max-width: 1180px) {
    .hero {
      min-height: 900px;
    }

    .hero-content {
      min-height: 900px;
      grid-template-columns: 1fr 0.72fr;
      padding: 120px 5% 390px;
    }

    .white-line,
    .wants-text {
      font-size: clamp(3.8rem, 6.4vw, 6.2rem);
    }

    .glow-word {
      font-size: clamp(2.9rem, 5vw, 5rem);
    }

    .hero-logo-wrap img {
      width: clamp(280px, 30vw, 430px);
    }
  }

  @media (max-width: 900px) {
    .lavender-line {
      flex-wrap: wrap;
      gap: 0.75rem;
    }
  }

  @media (max-width: 760px) {
    .hero {
      min-height: 920px;
    }

    .hero-content {
      min-height: 920px;
      grid-template-columns: 1fr;
      text-align: center;
      padding: 95px 1.4rem 330px;
      gap: 2rem;
    }

    .hero-copy {
      transform: translateY(0);
    }

    .hero-logo-wrap {
      transform: translateY(0);
    }

    .white-line,
    .wants-text {
      font-size: clamp(3.2rem, 12.8vw, 4.7rem);
    }

    .glow-word {
      font-size: clamp(2.8rem, 11vw, 4rem);
    }

    .hero-description {
      margin-left: auto;
      margin-right: auto;
      font-size: 1rem;
    }

    .hero-logo-wrap img {
      width: min(310px, 74vw);
    }

    .mountains {
      width: 150%;
      max-width: none;
      left: 50%;
      transform: translateX(-50%);
      bottom: -2px;
    }
  }
</style>
